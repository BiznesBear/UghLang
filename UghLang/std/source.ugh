# Testing function #
fun helloworld(){
	print "Hello world!";
}

# Returns string of fibbonaci sequance separeted with spaces #
fun fib(len){
    r = "";

    a = 0; 
    b = 1;  

    repeat len{ 
        c = (a + b); 
        r += (c + " "); 

        a = b;     
        b = c;
    }

    return r;
}
